# 事件篮页面头部间距设计

## 目标
使事件篮页面的灰色副标题与页面标题之间的间距，与今日事、奖励池等页面保持一致。

## 方案
调整 `src/pages/Inbox.tsx` 的头部 JSX 结构：将页面标题和副标题放入同一个左侧容器，新增事件表单保留在右侧。沿用现有 `.page-title` 和 `.page-subtitle` 样式，不新增页面专用间距补丁。

## 验收标准
- 事件篮标题下方副标题间距与其他页面一致。
- 新增事件输入框仍位于头部右侧，功能不变。
- 桌面端和窄屏响应式布局不出现回归。
